services:
  myalpine:
    image: alpine:latest
    command: ["-c", "ls -la /scripts"]
    entrypoint: /bin/sh
    volumes:
      - ./scripts:/scripts
      - test_data:/test_data

volumes:
  test_data:
